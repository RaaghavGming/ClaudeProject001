{
  "name": "claude-chatbot-app",
  "version": "1.0.0",
  "private": true,
  "description": "A web-based AI chatbot using React, Vite, Express, and the Anthropic Claude API.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
